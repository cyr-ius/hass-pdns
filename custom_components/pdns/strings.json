{
  "config": {
    "title": "PowerDNS Dynhost",
    "step": {
      "user": {
        "title": "DNS Dynamic Update (TSIG)",
        "data": {
          "pdns_server": "DNS Server (IP or hostname)",
          "dns_zone": "DNS Zone (e.g. example.com)",
          "dns_alias": "DNS Alias (FQDN, e.g. home.example.com)",
          "username": "TSIG Key Name",
          "password": "TSIG Key Secret (base64)",
          "tsig_algorithm": "Algorithm",
          "ttl": "TTL (seconds)"
        }
      }
    },
    "error": {
      "update_failed": "Update failed.",
      "login_incorrect": "Cannot connect to server or TSIG authentication failed.",
      "timeout": "Connection timeout.",
      "unknown": "Unknown error.",
      "detect_failed": "Unable to determine public IP address."
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_service%]"
    }
  }
}
